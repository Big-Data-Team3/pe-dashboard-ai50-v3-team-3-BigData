FROM python:3.11-slim

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 9000

ENV PYTHONPATH=/app

CMD ["uvicorn", "src.server.mcp_server:app", "--host", "0.0.0.0", "--port", "9000"]
